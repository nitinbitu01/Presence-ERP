# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
coverage
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Vendored face-api.js bundle + models (generated by scripts/vendor-face-api.mjs from the
# pinned @vladmandic/face-api devDependency — see "vendor:face-api" npm script and the
# postinstall hook in package.json). Not committed: regenerate with `npm run vendor:face-api`.
public/vendor/
public/models/

# ⚠️  SECURITY: Environment files — NEVER commit real secrets
# All .env variants must be ignored. Use .env.example for templates.
.env
.env.local
.env.*.local
.env.development
.env.staging
.env.production
# Explicitly allow the safe example template
!.env.example

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
